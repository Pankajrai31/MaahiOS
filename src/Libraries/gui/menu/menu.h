/**
 * MaahiOS - Menu API
 */

#ifndef MAAHI_MENU_H
#define MAAHI_MENU_H

/* Placeholder for future menu implementation */

/**
 * Create a menu (future enhancement)
 * @param window_id Parent window
 * @return Menu ID (>0) or -1 on error
 */
// int maahi_create_menu(int window_id);

/**
 * Add menu item (future enhancement)
 * @param menu_id Parent menu
 * @param text Menu item text
 * @return Item ID (>0) or -1 on error
 */
// int maahi_add_menu_item(int menu_id, const char *text);

#endif /* MAAHI_MENU_H */
